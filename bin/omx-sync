#!/usr/bin/env bash
set -euo pipefail

# Backward-compatible alias for previous launcher name.
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -n "${OMX_BIN:-}" && -z "${OMO_BIN:-}" ]]; then
  export OMO_BIN="$OMX_BIN"
fi

exec "$SCRIPT_DIR/omo-sync" "$@"
